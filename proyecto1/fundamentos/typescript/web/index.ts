// Nota1: El 80% del funcionamiento de typescript es js.
// Nota2: Usamos 'cd' para avanzar entre carpetas (ficheros), para devolvernos usamos 'cd ..'
// Nota3: Para ejecutar un archivo ts desde la terminal (preferencia git bash) usamos 'tsc nameFile.ts'

// typescript usa un compilador (traductor) -> traducción del codigo ts a código js

console.log('hola, im back perras');